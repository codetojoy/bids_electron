
## Quick log of commands

* `npm init`
* `npm install electron-prebuilt --save-dev`
* `npm install electron-packager --save-dev`

## to run locally

* `npm start`

## to build app

* `npm run build`
